import React from 'react'
import { shallow } from 'enzyme'

describe('login component', () => {
  it('renders login form', () => {
    //write test here
  })
  it('changes state when user types into email input', () => {
    //write test here
  })
  it('changes state when user types into password input', () => {
    //write another test here
  })
})
